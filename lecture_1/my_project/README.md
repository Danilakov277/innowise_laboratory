Colorama Demo Project
Проект для демонстрации работы с библиотекой Colorama - кросс-платформенной библиотекой для цветного вывода в терминале.

🎨 Особенности
Цветной вывод текста в терминале

Кросс-платформенная работа (Windows/Linux/macOS)

Простота использования и настройки

Полностью покрыт тестами

📋 Требования
Python 3.8 или выше

Poetry для управления зависимостями

🚀 Установка
Клонируйте репозиторий:

bash
git clone <your-repo-url>
cd colorama-project
Установите зависимости через Poetry:

bash
poetry install
Активируйте виртуальное окружение:

bash
poetry shell
💻 Использование
Запуск основной программы:
bash
poetry run python main.py
Вы увидите разноцветный вывод:

🔴 Красный текст на желтом фоне

🟢 Зеленый текст

🔵 Яркий синий текст

🟣 Пурпурный текст на голубом фоне

Программное использование:
python
from main import get_colored_text, print_colored_text

# Печать цветного текста
print_colored_text()

# Получение цветного текста как списка строк
colored_lines = get_colored_text()
for line in colored_lines:
    print(line)
🧪 Тестирование
Проект полностью покрыт тестами с использованием pytest.

Запуск тестов:
bash
# Все тесты
poetry run pytest

# С подробным выводом
poetry run pytest -v

# Тесты с покрытием кода
poetry run pytest --cov=main --cov-report=html

# Конкретный тестовый файл
poetry run pytest tests/test_colors.py -v
Структура тестов:
test_colors.py - основные тесты цветного вывода

test_integration.py - интеграционные тесты

Тесты проверяют наличие цветовых кодов, корректность вывода и работу функций

📁 Структура проекта
text
colorama-project/
├── main.py                 # Основной модуль с функционалом colorama
├── tests/                  # Директория с тестами
│   ├── __init__.py
│   └── test_colors.py      # Основные тесты
├── pyproject.toml          # Конфигурация Poetry и зависимостей
├── poetry.lock             # Файл блокировки версий
└── README.md              # Этот файл
🛠 Зависимости
Основные зависимости:
colorama (>=0.4.6) - для цветного вывода в терминал

pytest (>=7.0.0) - для тестирования

Dev-зависимости:
pytest-cov (>=4.0.0) - для измерения покрытия кода тестами

🔧 Настройка Colorama
Colorama автоматически инициализируется в функциях. Основные возможности:

python
from colorama import Fore, Back, Style, init

# Инициализация (автоматически в функциях)
init()

# Цвета текста (Foreground)
print(f"{Fore.RED}Красный текст")
print(f"{Fore.GREEN}Зеленый текст")
print(f"{Fore.BLUE}Синий текст")

# Цвета фона (Background)
print(f"{Back.YELLOW}Текст на желтом фоне")
print(f"{Back.CYAN}Текст на голубом фоне")

# Стили текста
print(f"{Style.BRIGHT}Яркий текст")
print(f"{Style.DIM}Тусклый текст")

# Сброс стилей
print(f"{Style.RESET_ALL}Обычный текст")